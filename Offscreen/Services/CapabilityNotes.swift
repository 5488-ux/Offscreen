import Foundation

enum CapabilityNotes {
    static let screenTime = "Screen Time 限制需要 FamilyControls、DeviceActivity、ManagedSettings，以及匹配的 Apple entitlement 和描述文件。"
    static let health = "健康奖励只读取用户授权后的步数、运动分钟、锻炼记录和活动能量。"
    static let video = "有效观看时长只在 App 前台且视频正常播放时累计。"
}
